extends layout

block content
  script(type='text/javascript').
    var shouldBroadcast = false;
    var room = "#{ params.room }";
    var initCall = #{ room_count > 0 };

  video#vid1(width='100%', height='100%')
  video#vid2(width='100%', height='100%')

